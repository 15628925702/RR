# RR-GID_CN

Minimal, reproducible project scaffold for the frozen RR-GID_CN research specification.

The implementation is intentionally limited to the P0 environment and configuration entry point. Research modules are added only after their preceding stage is accepted.

Run the CLI with `python -m rr_gid_cn --help` or `rr-gid-cn --help` after installing the package.

